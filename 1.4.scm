; This procedure makes use of an if statement to change which operator is used
; based on the input received. Interesting use to change not which of 2 code paths
; is executed, but the behavior of the one code path after the if statement
; If b is greater than 0, the procedure adds a and b
; If b is not greater than 0, the procedure subtracts b from a
